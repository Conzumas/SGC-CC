name: Trigger live UI fix

on:
  push:
    branches: [main]

permissions:
  contents: write

jobs:
  trigger:
    if: github.actor == 'Conzumas'
    runs-on: ubuntu-latest
    steps:
      - run: echo trigger
